{
  "name": "create-card-transaction-lambda",
  "version": "1.0.0",
  "description": "Função Lambda para processamento de mensagens do SQS e gravação no DynamoDB",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.1048.0" 
  }
}
